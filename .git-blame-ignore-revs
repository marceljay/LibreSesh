# Commits that only moved whitespace. `git blame` skips these so a line's
# history points at the change that wrote it, not the sweep that rewrapped it.
#
# Locally, once:  git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub reads this file on its own.

# style: sweep the tree with prettier, and wire the check into lint
b66f9006ba1773a6637e7c143708eca4b6d91a19
